package com.w00tmast3r.skript.lang;

/**
 * A utility class with methods pertaining to
 */
public final class Scripts {

    /**
     *
     * @param from the traversable to replace
     * @param to the replacement traversable
     * @return whether or not the injection succeeded
     */
    public static boolean inject(Traversable from, Traversable to) {
        return false; // TODO inject into Scripts
    }
}
