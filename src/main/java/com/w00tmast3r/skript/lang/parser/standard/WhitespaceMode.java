package com.w00tmast3r.skript.lang.parser.standard;

public enum WhitespaceMode {
    /**
     * Keep all whitespace.
     */
    RETAIN,
    /**
     * Collapse all whitespace into a single character.
     */
    COLLAPSE,
    /**
     * Remove all whitespace.
     */
    OMIT
}
